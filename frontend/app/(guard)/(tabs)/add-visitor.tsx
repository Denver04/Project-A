import { CustomText } from "@/components/CustomText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AddVisitor() {
  return (
    <SafeAreaView>
      <CustomText>Add Visitor</CustomText>
    </SafeAreaView>
  );
}